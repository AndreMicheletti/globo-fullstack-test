
const INITIAL_STATE = []

const articlesReducer = (state = INITIAL_STATE, action) => {
    console.log(action)
    switch (action.type) {
        default:
            return state
    }
}

export default articlesReducer
